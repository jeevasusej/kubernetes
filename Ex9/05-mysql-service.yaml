# Internal service. Other pods in person-app can connect to mysql:3306.
apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: person-app
spec:
  type: ClusterIP
  selector:
    app: mysql
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
